# AICC 상담 녹취록 RAG 추가 계획 (제4 콘텐츠: 상담사례)

작성 2026-07-27. 대상: `aicc/*.v3det.json`·`*.v3.txt` 134건 통화 전사를 기존 화면·업무·상담 3매뉴얼과 **융합**되는 검색 콘텐츠로 추가. 선례는 `상담매뉴얼_온라인추가_계획.md`(엑셀 Q&A → 온라인 전용)이며, 본 계획은 그 격리 패턴을 그대로 계승하되 **비정제 대화 데이터**라는 차이에서 오는 청킹·정제·STT 보정 설계를 추가한다.

상태: **계획 확정 — 착수 대기**. 사용자 결정 반영(2026-07-27): ①**PII 마스킹 확정**(실명 포함) ②정제 LLM은 **정액제 Claude Code CLI 기반** ③`aicc/`·정제 산출물 **git 제외 확정**(.gitignore/.dockerignore 등재 완료) ④서빙 결과는 상담 원문이 아닌 **핵심질문+명확한 정답 카드만** — 재현율·정확률 동시 최적화(§4.4). 잔여 결정은 §10.

---

## 1. 데이터 실사 결과 (2026-07-27)

| 항목 | 값 | 시사점 |
|---|---|---|
| 통화 수 | 134건 (총 1.6MB, 세그먼트 1,697개) | 소규모 — 통화별 LLM 정제 패스 비용 감당 가능 |
| 파일 3종 | `.v3det.json`(세그먼트+role+타임스탬프) / `.v3det.txt`(role 태깅 원전사) / `.v3.txt`(**교정본**) | `.v3.txt`가 이미 1차 보정 완료본("1번"→"한번", 문장부호, 띄어쓰기). **텍스트 소스는 `.v3.txt`**, role·타임스탬프는 `.v3det.json`에서 |
| 통화 길이 | 중앙값 83초·9세그먼트, 최대 597초·62세그먼트 | 대부분 **단일 이슈 통화** → 통화=기본 의미 단위. 장통화만 분할 필요 |
| 역할 라벨 | `role_resolved=false` 13건(10%), `[미상]` 라벨 잔존. 세그먼트 병합으로 **중도 화자 스왑** 존재(예: 0b9b3bb6 — 고객 발화가 [상담사]로) | role 라벨을 그대로 믿을 수 없음 — 정제 패스에서 재판정 |
| 저품질 통화 | 200자 미만 30건(22%) — 콜백 안내·오인식 잡음 등 | 사례 가치 판정 게이트로 제외 → 색인 대상 실질 ~90–105건 예상 |
| ASR 신뢰도 | avg_logprob 중앙값 -0.259, 하위 10% -0.369 | 저신뢰 세그먼트 마킹 → 보정·검수 우선순위 |
| 화면번호 | 4자리 후보 68종 중 **43종이 실존 screen_no와 일치**(chunks.jsonl 3,950종 대조). 불일치 25종은 에러코드(2707)·펀드번호·부서번호 | 화면매뉴얼 크로스링크 가치 큼. 단, 숫자만으로는 오탐 — 문맥("NNNN 화면/번") + 실존 집합 대조 필수 |
| PII | 상담사 실명(인사말 "고객지원센터 OOO입니다"), 고객 실명, 고객사명(IBK·BNK·삼성자산운용…), 부서·지점번호, 계좌 언급 | 기존 `mask_pii` 3종만으로 부족 — 발화체 전용 규칙 확장 필요(§6) |
| STT 오류 예 | "친절하게 모시겠습니다"→"친절하겠습니다", 상담사명 변형(동일 상담사명이 2종 표기로 전사), "여시면"→"열으시면" | 반복 패턴 사전화 가능(§5) |

`aicc/`는 현재 **git untracked·ignore 미등록** 상태 — 원본 민감자료이므로 0단계에서 즉시 `.gitignore`/`.dockerignore` 등재.

---

## 2. 배치 위치 결정: 온라인 전용 (상담매뉴얼과 동일 승인선) — 권장

- 실명·고객사명·계좌 언급이 구어로 산재한 **실데이터**. 상담매뉴얼과 동일하게 **접근키 게이트 뒤 온라인 전용**이 승인선과 정합(익명 fail-closed, `상담매뉴얼_온라인추가_계획.md` §9 선례).
- 격리 메커니즘도 동일 계승: 산출물을 로컬 `data/chunks.jsonl`과 **물리적으로 분리된 `data/chunks_aicc.jsonl`**에 쓰고, `ingest_real.py`에서만 병합 업서트. 로컬 FAISS 색인(`build_index.py`는 `chunks.jsonl`만 읽음)·`src/webapp.py`·파서 골든은 무접촉.
- 로컬(사내 서버) 편입은 PII 마스킹 신뢰도가 감사 리포트로 검증된 뒤 **별도 결정**(범위 밖). 그때는 `web/app.js`에 상담·사례 배지/무링크 처리부터 이식해야 함(온라인 프런트에는 이미 있음).
- LLM 경계 (**사용자 확정**): 정제·보정 LLM은 **Claude Code CLI**(`claude -p` 헤드리스 배치, 정액제 — 건당 비용 0) 기반. 단 이는 `deploy/online/` 구역의 **"로컬 실행 도구 — 배포 제외"** 전처리 전용이다 — 2026-07 안전 계약(웹 답변 백엔드 `none|ollama`만 허용, webapp CLI 예외 폐기)은 그대로 유지되며, 런타임 답변 경로에는 일절 관여하지 않는다. 데이터 유출 관점에서도 aicc는 온라인 업로드(OpenAI 답변 경로) 확정 데이터라 동일 승인선 내. CLI 부재 환경 폴백은 ollama(qwen2.5:7b) — 산출물 스키마 동일.

## 3. 스키마 결정: 신규 manual 값 `"상담사례"` (id 접두 `cc:`)

기존 `"상담"`(엑셀 Q&A) 재사용이 아닌 **신설**을 권장한다. 근거:

1. **신뢰 등급이 정반대**: 상담 Q&A는 "검증된 정답 — A. 원문 그대로 답하라"(`_common.py` 실데이터 프롬프트). 녹취 사례는 비검증 실황 — 상담사가 즉석에서 틀리게/불완전하게 안내했을 수 있고 미해결 통화도 있다. 같은 manual이면 프롬프트에서 등급 분리가 불가능하다.
2. `parse_counsel_xls.py`는 `sector_path==["상담",업무]` 2-level을 assert — 사례를 끼워 넣으면 기존 파서·검증 회귀 위험.
3. 스코프 트리·필터·모호성 배너는 `manual`/`sector_path`만 보는 범용 로직이라 4번째 값 추가에 무수정 동작(업무·상담 추가 때 2회 검증된 사실).

청크 스키마는 표준 15필드 그대로:

- `id`: `cc:{recording_id 앞 8자}:{0001}`
- `manual`: `"상담사례"`, `sector_path`: `["상담사례", 부문]` — 부문은 화면번호 매칭 시 해당 화면의 sector 상속, 없으면 정제 LLM이 분류(계좌/주문/펀드/수신…), 판정 불가면 `["상담사례"]` 1-level
- `screen_no`/`screen_id`: 검증된 화면번호 매칭 시 부여(→ related·근거 지도·크로스링크가 무수정 동작). **screen_no가 주인공, screen_id는 내부 기능값** 규칙 준수
- `chunk_type`: `"qa"`(사례 카드 — 질문뱅크·칩 생태계 재사용), 원문 발췌 보조 청크를 만들 경우 `"description"`
- `source_url`: 빈 값(온라인 프런트는 무링크 처리 기존 지원). `title`: "상담사례: {이슈 한 줄}"

## 4. 청킹 전략 — 대화 데이터의 핵심 설계

**발화(turn) 단위 청킹은 채택하지 않는다.** 근거: 발화 하나는 문맥 없이는 무의미("네네", "잠시만요"), ASR 오류·화자 스왑이 발화 단위에서 가장 심하며, 검색 질의는 "OOO 하려면 어떤 화면?" 같은 질문형이라 Q→A 구조와 매칭돼야 한다.

채택: **통화 → 이슈 에피소드 → 구조화 사례 카드**의 2층 구조.

### 4.1 1층 — 검색 대상: 사례 카드 (LLM 정제 산출물)

통화당 1~N개(중앙값 1). 정제 LLM(§2 백엔드)이 통화 전문을 읽고 산출:

```json
{
  "issue": "특정 계좌에 특정 종목만 보이게 조회하는 화면이 있는지 문의",
  "screen_nos": ["5333", "5339"],            // 문맥+실존 대조 검증 통과분만
  "situation": "회원사 담당자가 고객 요청으로 종목별 계좌 확인 화면을 찾음",
  "resolution": "해당 기능의 전용 화면은 없음을 안내",
  "resolved": true,                            // false = 미해결/추가확인 통화
  "sector_guess": "계좌",
  "quality": "ok" | "low"                      // low = 사례 가치 없음 → 색인 제외
}
```

- `text` = **"Q. {핵심질문}\nA. {명확한 정답}"만** — 대화 원문·발췌를 서빙 청크에 싣지 않는다(**사용자 확정**: 상담 내용 노출 대신 정제 Q&A만 결과로). 원문 발췌·근거 대응은 2층 아카이브(§4.2)에만 보존해 감사·검수 시 추적한다.
- `embed_text` = `[상담사례/{부문}] 화면번호 {NNNN} {핵심질문} : {정답 요지}` + **구어 질문 변형 병기**(§4.4 재현율 장치, 2000자 절단). '화면번호 NNNN' 명시 주입은 화면 표기 규칙과 동일 목적 — LLM이 문서코드·에러코드를 화면번호로 오답하지 않게 함.
- **장통화 분할**: 세그먼트 20개 초과 또는 300초 초과 통화는 정제 LLM이 이슈 전환점 기준으로 에피소드 분할(최대 3개) — 카드가 각각 생성됨. 임의 길이 절단은 하지 않는다(문맥 파괴).
- **색인 자격 = 정답 명확성**(사용자 확정): `resolved=true`이고 정답이 통화 내에서 명시적으로 확인되는 카드만 색인한다. 미해결·모호·추측성 안내 통화는 색인 제외하고 제외 사유를 리포트에 남긴다(추후 상담매뉴얼 보완 소재로 활용). 저품질 판정과 합쳐 색인 대상은 전량의 절반 안팎으로 예상 — 정확률 우선이며, 재현율은 §4.4 장치로 보전한다.

### 4.1-1 자동 반영 게이트 + 판정 대장 (사용자 확정: STT 저품질·불명확 통화는 반영 금지, 반영/미반영 추적 필수)

색인 여부는 통화별로 **자동 판정**하고, 전건을 **판정 대장 `data/aicc_ledger.jsonl`**(통화 1건=1행)에 기록한다:

- **자동 제외 기준(정제 LLM 이전에 걸리는 결정적 게이트)**: ①텍스트 200자 미만 ②avg_logprob 통화 평균이 임계 미달(파일럿에서 보정, 초안 -0.35) ③role 재판정 후에도 화자 구분 불능 — 사유코드 `stt_short` / `stt_lowconf` / `role_unresolved`.
- **정제 단계 제외 기준(LLM 판정)**: ④정답 불명확·미해결(`unresolved`) ⑤사례 가치 없음—단순 콜백·인사(`no_value`) ⑥사실 확신 불가(`ambiguous`).
- **대장 스키마**: `{recording_id, status: "indexed"|"excluded", reason: [사유코드], metrics: {chars, avg_logprob, segments, role_resolved}, card_ids: ["cc:..."], masked: true, refined_at}` — `parse_aicc.py`는 대장의 `indexed` 행만 청크로 변환하므로 **대장이 곧 반영 여부의 단일 진실 소스**. 재실행 시 갱신 이력은 리포트에 전/후 대조로 남긴다.
- **현황 리포트**: 보정 diff 리포트에 반영 N건 / 미반영 M건(사유별 집계) + 건별 목록 섹션 포함 — 나중에 어떤 통화가 왜 빠졌는지 즉시 추적 가능. 대장·리포트 모두 `data/` 하위라 git 자동 제외.

### 4.2 2층 — 검색 비대상: 정제 전사 아카이브

`data/aicc_refined/{recording_id}.json`에 role 재판정·STT 보정 반영본 + 원전사 + 세그먼트 타임스탬프 보존. 색인·업서트하지 않으며 사례 카드의 근거 추적(감사·검수·재정제)용. 추후 프런트 "원문 보기" 패널을 붙일 때의 재료(범위 밖).

### 4.3 재현율·정확률 동시 설계 (사용자 확정 요구)

정확률은 "적게, 확실하게 색인"으로, 재현율은 "색인한 것을 넓게 매칭"으로 달성한다 — 서로 다른 층에서 해결하므로 상충하지 않는다.

**정확률(Precision) 장치**
1. 색인 자격 게이트(§4.1-1): 정답 명확 카드만 색인 — 오안내·미해결이 정답처럼 검색될 경로 원천 차단.
2. 화면번호는 실존 대조 통과분만 부여 — 환각 번호로 인한 오답·오링크 차단.
3. **자기-검색 검증**: 카드의 핵심질문으로 실제 검색해 자기 카드가 top-3 진입 + 타 부문 오염 없음 확인(질문뱅크 `gen_questions.py` 검증 로직 재사용) — 실패 카드는 embed_text 보강 또는 색인 보류.
4. 프롬프트 등급 분리(§7-2): 매뉴얼·상담 근거 우선, 사례는 보조.

**재현율(Recall) 장치**
1. `embed_text`에 **정규화 질문 + 고객 구어 표현 병기**: "Q. 특정 계좌의 특정 종목만 조회하는 화면이 있는지 (고객 표현: 한 종목만 보이게 확인 가능한 화면)" — 표준어 질의와 구어 질의 양쪽에 매칭. BM25(자모 unigram+bigram)와 dense 모두에 유효.
2. 검증된 화면번호·용어사전 표준 용어를 embed_text에 주입 — "5333" 같은 번호 질의로도 직접 히트.
3. 카드 질문의 질문뱅크 합류(§7-3) — 추천 칩 경유 도달률 보강.
4. 재현율 측정: 파일럿 검수 시 카드별 "이 사례를 찾을 법한 질의 2종"(표준어 1·구어 1)을 함께 만들어 Recall@5 측정 세트로 축적 — 3단계 수용 기준에 포함.

### 4.3-1 정제 캐시 패턴

`extract_pm_images.py`→`pm_image_text.json` 캐시 패턴 계승: 정제 결과를 `data/aicc_cards.json`에 recording_id 키로 캐시, 파서(`parse_aicc.py`)는 캐시만 읽어 결정적으로 청크 생성 → 골든 테스트 가능(LLM 비결정성이 테스트에 안 들어옴).

## 5. STT 오탈자 보정 — 3층 파이프라인 + 검수 루프

원칙: **원문 전사는 불변, 보정은 파생물에만**. 교정 내역은 전건 로그로 남겨 재현·감사 가능하게 한다.

- **L0 소스 선택**: `.v3.txt`(기보정본)를 텍스트 소스로. role은 `.v3det.json`에서 발화 순서 정렬로 근사 매핑(통화 단위 처리라 세그먼트 어긋남 허용).
- **L1 사전·규칙 보정 (결정적, LLM 불요)**:
  - **도메인 용어사전 자동 구축**: `data/chunks.jsonl`에서 화면명(title)·glossary term·screen_no 3,950종 + PowerBASE 고유명사(파워베이스, K-Front, 미수, 반대매매, 예탁금이용료…)를 추출해 `data/aicc_lexicon.json` 생성. 매뉴얼 코퍼스가 곧 정답 어휘집 — 별도 수작업 사전 불요.
  - **자모 분해 편집거리** fuzzy 매칭으로 용어 근접 오기 교정(임계 보수적으로, 확신 없으면 미교정).
  - **화면번호 검증**: 4자리 후보를 문맥 패턴("NNNN 화면/번(을) 보", "화면번호") + 실존 screen_no 집합 대조로만 확정. 불일치 후보는 교정하지 않고 카드 `screen_nos`에서 제외만(에러코드·펀드번호 오탐 방지 — 실사에서 25/68종이 비화면번호).
  - **반복 오류 패턴표**: 인사말("친절하겠습니다"→"친절하게 모시겠습니다") 등 전건 공통 패턴을 `aicc_fixes.json`으로 명시 관리 — 검수에서 발견될 때마다 추가하는 축적형.
- **L2 LLM 문맥 보정 (Claude Code CLI)**: 통화별로 용어사전 상위 매칭 후보를 프롬프트에 주입해 "전사 오류로 보이는 어절만 사전 어휘로 정정, 내용 변경 금지" 재작성. avg_logprob 하위 세그먼트를 명시 표시해 집중 검토시킴. 동시에 **role 재판정**(인사말 발화자=상담사, 존대·안내 방향 휴리스틱 포함) — `role_resolved=false` 13건과 스왑 통화 복구.
- **L3 검증 루프 (수용 게이트)**: 보정 전/후 diff 웹리포트 생성(`report_counsel_pii.py`의 건별 대조 패턴 재사용) → 샘플 수작업 검수 → 오교정은 L1 패턴표·사전에 반영 후 재실행. **교정률·오교정률을 리포트에 수치로 표기**하고, 오교정 발견 0건이 될 때까지 파일럿 반복.

## 6. PII 마스킹

- `parse_counsel_xls.py`의 `mask_pii` 3종(카드·계좌·식별번호)을 import 재사용 + **발화체 확장**: ①상담사 실명 — 인사말 패턴("고객지원센터 (OOO)입니다") 탐지해 성만 남기고 마스킹 ②통화 중 언급 고객·담당자 실명 ③전화번호 구어 낭독 패턴. 정제 LLM 카드 생성 프롬프트에도 "실명은 카드에 옮기지 말 것" 지시(이중 방어).
- **마스킹 확정(2026-07-27 사용자 결정)**: 상담사·고객·담당자 **실명, 전화번호, 계좌·식별번호는 전부 마스킹**. 서빙 청크는 정제 Q&A만 담으므로(§4.1) 인명이 카드에 들어갈 이유 자체가 없다 — 카드 생성 프롬프트에 인명 금지 지시 + 산출물 재스캔 이중 방어. 고객사명은 상담매뉴얼 선례(접근키 게이트 전제 보존) 계승하되 카드에는 필요한 경우에만 유지.
- 수용 게이트: `report_aicc_pii.py`(654줄짜리 기존 도구 구조 벤치마킹, 건별 잔존 재탐지·원본 잔존·배포 반영 3중 자동검증) 통과 전 업서트 금지.

## 7. 융합 — "전반적 퀄리티 상승"을 만드는 연결점 4가지

1. **화면매뉴얼 크로스링크**: 카드에 검증된 screen_no·sector가 붙으므로 기존 related 확장(화면→부문→매뉴얼)·근거 지도·스코프 필터가 **무수정으로 사례까지 동작**. 답변에서 "매뉴얼 근거 [S1] + 실제 상담사례 [S3]" 병기가 자연 발생.
2. **프롬프트 등급 분리**: `_common.py` SYSTEM_PROMPT에 추가 — "상담사례 근거(경로 '상담사례 > …')는 실제 통화의 **참고 사례**다. 매뉴얼·상담 근거와 함께 있으면 매뉴얼을 우선하고 사례는 '실제 문의 사례로는…'으로 보조 인용하라. 사례만 있으면 검증되지 않았음을 밝혀라." — 상담 Q&A("검증된 정답, 원문 그대로") 지시와 명시적으로 등급 차등.
3. **질문뱅크 합류**: 카드 issue(실제 고객의 구어 질문 표현)를 `gen_questions.py` 후보에 투입, **자기-검색 검증 통과분만** 편입 — 기존 881건 뱅크에 없는 구어체 표현("미수라고 빨갛게 뜨는데")이 들어와 추천 칩·구어 질의 recall이 실질 개선. (상담매뉴얼 계획의 미착수 4단계와 같은 트랙 — 함께 처리 가능.)
4. **빈도 신호**: 카드에 동일 이슈 사례 수가 누적되면 "자주 묻는 이슈" 신호로 활용 가능(질문 추천 우선순위) — 134건 규모에선 관찰만, 녹취 증분 유입 시 활성화.

교차 오염 방어: 스코프 미지정 검색에서 사례 카드가 매뉴얼 정답을 밀어내면 안 됨 → §9 검증에서 상담매뉴얼 추가 때와 동일한 leak 측정(기존 오염 0 유지가 기준). 필요시 사례는 rerank 동점 시 후순위 정도의 소극적 패널티만 검토(선제 적용 안 함).

## 8. 파이프라인·파일 구성

```
aicc/*.v3.txt + *.v3det.json
  → deploy/online/refine_aicc.py    # 자동 게이트 → L1+L2 보정 → role 재판정 → 카드 생성 (LLM: Claude Code CLI, 폴백 ollama)
      → data/aicc_lexicon.json       # 매뉴얼 코퍼스 추출 용어사전 (자동 생성)
      → data/aicc_fixes.json         # 반복 오류 패턴표 (수동 축적)
      → data/aicc_cards.json         # 정제 캐시 (recording_id 키, 결정적 재파싱의 입력)
      → data/aicc_ledger.jsonl       # 판정 대장 — 반영/미반영·사유코드 전건 추적 (§4.1-1)
      → data/aicc_refined/*.json     # 2층 아카이브
  → deploy/online/parse_aicc.py     # 대장 indexed 행 + 캐시 → 표준 청크 (LLM 불요·결정적)
      → data/chunks_aicc.jsonl
  → deploy/online/report_aicc_pii.py + 보정 diff 리포트   # 수용 게이트
  → deploy/online/ingest_real.py     # chunks.jsonl + chunks_counsel.jsonl + chunks_aicc.jsonl 순 연결 업서트(.ingest_state 증분)
```

- 테스트: `tests/test_parse_aicc.py`(캐시 고정본 기반 골든 — 카드 수·id 접두·sector_path 형태·embed_text 접두·마스킹 잔존 0), `test_online_data_boundary.py`에 합성 공개모드에서 `manual="상담사례"` 누출 시 거부되는 위반 케이스 추가(기존 `manual='상담'` 위반 테스트 L233과 동형).
- 평가: 상담매뉴얼 배포 때의 온라인 스코프 gate·오염 검증 절차 재실행 + `eval_scope.py`의 `MANUAL_CROSS_QUERIES` 패턴으로 사례↔화면 교차 질의 세트 작성(온라인 측정).

## 9. 단계별 실행 계획 (각 단계 끝 = 수용 게이트, 실행은 Opus/Sonnet 위임)

| 단계 | 내용 | 수용 기준 |
|---|---|---|
| **0. 격리·안전** | ~~`aicc/` gitignore·dockerignore 등재~~(2026-07-27 완료), 경계 테스트 추가, 용어사전 생성기 | `make test` 통과, 패키징 경계 테스트에 aicc 포함 |
| **1. 파일럿 20건** | 대표 20건(장·단·미상·저품질 포함) 게이트→L1→L2→카드 생성, 대장·diff·PII 리포트, 수작업 검수 | 카드 사실 왜곡 0건, 오교정 0건, role 오판 0건, **자동 게이트 판정과 수작업 판정 일치**(불일치 시 임계 재보정). 실패 시 사전·패턴표 보강 후 재실행 |
| **2. 전량 134건** | 파일럿 확정 설정으로 전량 처리, `chunks_aicc.jsonl` 생성, 골든 테스트 작성 | PII 3중 검증 전건 통과, **대장 134건 전건 기록**(indexed+excluded=134, 사유코드 무결) |
| **3. 업서트·검증** | ingest 증분 업서트, 스코프 gate·교차 오염·τ 재검증, 카드별 질의 세트 Recall 측정 | 기존 오염 0 유지, τ=0.70 게이트 정상, 기존 질의 회귀 없음, **자기-검색 top-3 + 구어 질의 Recall@5 통과**(§4.3) |
| **4. 융합 기능** | 프롬프트 등급 지시, 프런트 `상담사례` 배지·스코프 노출, 질문뱅크 합류(자기-검색 검증) | 사례+매뉴얼 병기 답변 스모크 통과, 뱅크 편입분 재검증 통과 |

## 10. 사용자 결정 현황

| 항목 | 상태 |
|---|---|
| PII 마스킹 | **확정(2026-07-27)** — 실명·전화·계좌 마스킹, 서빙 카드에 인명 금지(§6) |
| 정제 LLM 백엔드 | **확정(2026-07-27)** — Claude Code CLI(정액제) 기반, 폴백 ollama. 런타임 답변 경로 무관(§2) |
| git 커밋 경계 | **확정·적용 완료(2026-07-27)** — `aicc/`·`data/aicc_refined/` gitignore, dockerignore 등재 |
| 서빙 형태 | **확정(2026-07-27)** — 상담 원문 비노출, 명확한 Q&A 카드만 색인. STT 저품질·불명확 통화 자동 제외 + 판정 대장 추적(§4.1-1) |
| 로컬(사내 서버) 편입 여부 | **미정** — 본 계획은 온라인 전용. 로컬 편입은 PII 검증 후 별도 계획 |

## 11. 리스크

| 리스크 | 방어 |
|---|---|
| 상담사의 즉석 오안내가 정답처럼 검색됨 | 미해결·불명확 통화 색인 제외(§4.1-1) + manual 등급 분리·프롬프트 차등(§7-2) + 매뉴얼 근거 우선 지시 |
| 색인 축소(≈절반)로 재현율 하락 | §4.3 재현율 장치(구어 병기 embed_text·질문뱅크 합류) + 3단계 Recall 측정 게이트. 제외 통화는 대장에 남아 임계 완화 재검토 가능 |
| ASR 환각 화면번호가 카드에 유입 | 실존 screen_no 집합 대조 통과분만 부여(§5 L1) |
| LLM 정제가 사실을 지어냄 | 카드에 원문 발췌 동봉, 파일럿 전건 수작업 검수, 캐시 고정으로 재현성 확보 |
| 사례가 매뉴얼 정답을 밀어내는 오염 | 3단계 leak 측정 게이트(기존 0 유지 기준) |
| 원본 유출(git) | 0단계 ignore 등재 + 패키징 경계 테스트 |
| 상담 Q&A와 동일 이슈 중복 | 허용(사례는 보조 등급이라 무해) — 임베딩 유사도 0.95+ 중복만 카드 생성 시 병합 검토 |
