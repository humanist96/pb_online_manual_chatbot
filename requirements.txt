# PowerBASE 계좌 매뉴얼 RAG 챗봇 — 전부 오픈소스/로컬
# 설치(권장, uv):
#   uv venv --python 3.12 .venv
#   uv pip install --python .venv/bin/python -r requirements.txt \
#       --extra-index-url https://download.pytorch.org/whl/cpu

# 파싱/변환
beautifulsoup4>=4.13
lxml>=5.0
openpyxl>=3.1

# 임베딩/검색/리랭크 (로컬)
sentence-transformers>=3.0     # 임베딩 + CrossEncoder 리랭커(BAAI/bge-reranker-v2-m3) 로딩
faiss-cpu>=1.8
rank-bm25>=0.2
# 리랭커는 관련도 게이트(무관 질의 차단)에 사용 — 추가 패키지 불필요(sentence-transformers 로 로딩).
# RERANK_ENABLE=off 로 끄면 코사인 임계치만 사용.

# LLM 서빙은 Ollama(별도 설치, https://ollama.com)를 사용한다.
#   ollama pull qwen2.5:7b-instruct      # 또는 exaone3.5:7.8b
